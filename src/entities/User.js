class User {
  constructor(uid, firstName, lastName, email, phoneNumber, gender) {
    this.uid = uid;
    this.firstName = firstName;
    this.lastName = lastName;
    this.email = email;
    this.phoneNumber = phoneNumber;
    this.gender = gender;
  }
}

export default User;