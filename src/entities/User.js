class User {
  constructor(uid, firstName, lastName, gender, userEmail) {
    this.uid = uid;
    this.firstName = firstName;
    this.lastName = lastName;
    this.gender = gender;
    this.userEmail = userEmail;
  }
}

export default User;